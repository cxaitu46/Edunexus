# MongoDB (MERN data layer)
MONGODB_URI="mongodb://localhost:27017/edunexus"

# Express port — serves API and the React app (same origin).
PORT=4004

# JWT secret for auth tokens
JWT_SECRET="your_jwt_secret_here"

